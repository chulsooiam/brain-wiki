# Overview

This is the vault's front page. Use it to describe what this knowledge base is,
who it belongs to, and its main areas once you begin ingesting sources.
[[index]] (`wiki/index.md`) is the master index, [[hot]] (`wiki/hot.md`) is the
session cache, and [[log]] (`wiki/log.md`) is the ingest log.
